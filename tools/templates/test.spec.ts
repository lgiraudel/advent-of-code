import { puzzle1, puzzle2 } from '.'

const sample = ``.split('\n')

describe('Day __day__', () => {
    test('Puzzle __day__.1', () => {
        expect(puzzle1(sample)).toBe('something')
    })

    test('Puzzle __day__.2', () => {
        expect(puzzle2(sample)).toBe('something')
    })
})